// Workspace target for the `anipres/anipres.css` export. CSS Modules
// are pulled in transitively via component imports — this file only
// needs to mirror the globals from Anipres.tsx.
import "tldraw/tldraw.css";
import "./tldraw-overrides.css";
