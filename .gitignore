# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Worker local secrets
.dev.vars
.wrangler/

# Local MCP config — may contain API keys per-host. Track a templated
# version manually if/when the team wants to share an `mcp.json`.
.mcp.json

# Local dev caches that pnpm / Playwright MCP create alongside the repo.
.pnpm-store/
.playwright-mcp/

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
